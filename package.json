{
  "name": "nodejscrash",
  "version": "1.0.0",
  "description": "crash course because am retarted",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --max-old-space-size=4096 server.js",
    "start:prod": "node --max-old-space-size=4096 server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sspark",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
